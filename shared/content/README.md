# shared/content

Executable content schemas, registry contracts and dependency validation as consumers are implemented. Prose contract is docs/CONTENT_SYSTEM.md.

M1 `arena.ts` defines pinned versions, local Arena configuration and bounded runtime validation. General content dependency/schema registries remain later work; registered capabilities must replace arbitrary executable content.
