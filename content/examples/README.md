# Design examples

These JSON documents are planning fixtures, not runtime content and not promises of existing schemas. Their runtimeDefinition flag must remain false. Build typed definitions and validators alongside actual registered consumers; follow the content specification.
